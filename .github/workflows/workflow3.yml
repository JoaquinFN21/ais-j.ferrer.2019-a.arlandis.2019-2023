on:
  push:
    branches:
    - master
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'adopt'
          java-version: '17'
      - name: Run tests
        run: mvn test
      - name: Generate Docker image
        run: mvn spring-boot:build-image -DskipTests -Dspring-boot.build-image.imageName=${{ repo2001 }}/items:v1
      - name: Login to DockerHub
        run: docker login -u "${{ repo2001 }}" -p "${{ dckr_pat_nRDSnO9P4rWSpgM1aDy58B6n-SU }}" 
      - name: Push image to DockerHub
        run: docker push ${{ repo2001 }}/items:v1


